# TinyZKP reconciliation deploy runbook — retired

This 2026-06-23 runbook is retained only as a tombstone. Its proving,
shared-worker, legacy billing, virtualenv, Wrangler, and deployment commands
were retired during backend recovery and must not be executed.

Use the current maintenance-only operator procedure in
[`docs/operations.md`](../operations.md) and invoke
`scripts/ci/run_production_preflight.sh` directly. Historical content remains
available in Git history at the commit preceding the backend-recovery
containment change.
