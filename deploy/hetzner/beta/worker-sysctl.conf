# Keep scratch-heavy proof jobs from filling a small job cgroup with dirty
# page cache before Linux starts writeback. The proving host has substantially
# more physical RAM than each bounded job, so ratio-based defaults are unsafe.
vm.dirty_background_bytes = 134217728
vm.dirty_bytes = 268435456
