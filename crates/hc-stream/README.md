# hc-stream

`hc-stream` provides TinyZKP's MIT-licensed, versioned contracts for resource
policies, block-readable matrices, scratch-backed stores, artifact digests,
and deterministic checkpoints.

It contains no proof protocol and performs no network operations.
