 [package]
 name = "hc-verifier"
 version.workspace = true
 edition.workspace = true
 rust-version.workspace = true
 license.workspace = true
 publish = false

 [dependencies]
hc-core = { path = "../hc-core" }
hc-air = { path = "../hc-air" }
hc-fri = { path = "../hc-fri" }
hc-hash = { path = "../hc-hash" }
hc-prover = { path = "../hc-prover" }
thiserror.workspace = true

[dev-dependencies]
hc-vm = { path = "../hc-vm" }
hc-sdk = { path = "../hc-sdk" }
# Test-only: used by the forge_poc_g2 module (BlockProducer/VecBlockProducer to
# feed the prover's FRI path; MerklePath to populate an unchecked witness).
hc-replay = { path = "../hc-replay" }
hc-commit = { path = "../hc-commit" }
proptest = "1.4"

